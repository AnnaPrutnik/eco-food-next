import React from 'react';
import { Navbar } from '@nextui-org/react';
import { Box } from './Box';

export const Header = () => {
  return (
    <Box as="header">
      <Navbar disableShadow height={{}}></Navbar>
    </Box>
  );
};
